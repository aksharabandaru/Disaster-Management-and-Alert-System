-- Create the disaster_data database
CREATE DATABASE IF NOT EXISTS disaster_data;

-- Use the database
USE disaster_data;

-- Show confirmation
SELECT 'Database disaster_data created successfully!' as status;
